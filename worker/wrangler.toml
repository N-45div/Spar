name = "spar-api"
main = "src/index.ts"
compatibility_date = "2026-08-01"

# Per-IP rate limit (free plan). A full round is ~4 requests per turn.
[[unsafe.bindings]]
name = "RL"
type = "ratelimit"
namespace_id = "1001"
simple = { limit = 120, period = 60 }
